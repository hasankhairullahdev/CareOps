apiVersion: v1
kind: ConfigMap
metadata:
  name: pharmacy-service-config
  namespace: careops
data:
  ASPNETCORE_ENVIRONMENT: Production
  Keycloak__Authority: "http://keycloak:8080/realms/careops"
  Keycloak__Audience: "account"
  RabbitMQ__Host: rabbitmq
  RabbitMQ__Username: guest
  RabbitMQ__Password: guest
---
apiVersion: v1
kind: Secret
metadata:
  name: pharmacy-service-secret
  namespace: careops
type: Opaque
stringData:
  ConnectionStrings__DefaultConnection: "Host=postgres;Database=pharmacy_db;Username=postgres;Password=postgres"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pharmacy-service
  namespace: careops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pharmacy-service
  template:
    metadata:
      labels:
        app: pharmacy-service
    spec:
      containers:
        - name: pharmacy-service
          image: ghcr.io/hasankhairullahdev/careops/pharmacy-service:latest
          ports:
            - containerPort: 5003
          envFrom:
            - configMapRef:
                name: pharmacy-service-config
          env:
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: pharmacy-service-secret
                  key: ConnectionStrings__DefaultConnection
            - name: Urls
              value: "http://+:5003"
          readinessProbe:
            httpGet:
              path: /health
              port: 5003
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: pharmacy-service
  namespace: careops
spec:
  selector:
    app: pharmacy-service
  ports:
    - port: 5003
      targetPort: 5003
